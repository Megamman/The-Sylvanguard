using UnityEngine;

public class FindBiulder : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        StoryStatic.T2 = true;

        Debug.Log("Tier 2 Open");
    }

}
